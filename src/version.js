// App version, shown in the top-left corner (BuildTag) so you can tell whether
// the phone is running the latest deploy or a cached build.
//
// Semantic versioning, MAJOR.MINOR.PATCH — bump in every PR:
//   MAJOR  a big overhaul (rare)          (resets MINOR and PATCH to 0)
//   MINOR  a new feature                  (resets PATCH to 0)
//   PATCH  a bug fix, tweak or polish
export const APP_VERSION = '1.2.0'
